MYSQL:
### Difference between the ROW_NUMBER, DENSE_RANK(),RANK()
Answer: 
      1. These 3 are used when we use window functions in the MYSQL 
      2. lets take one example easy to exaplin as well 
      if we do partition BY and OrderBY age .
            Row_number: Gives unique sequential numbers (Top 1 per group)
                        No two rows get the same number
            Rank():
                  Same values get same rank
                  But ranking will have gaps
            DENSE_RANK():
                        Same values get same rank
                        But ranking will NOT have gaps

            Complexity: ROW_NUMBER==DENSE_RANK()<RANK()

###MYSQL Execution order
1.FROM
2️⃣ JOIN
3️⃣ WHERE
4️⃣ GROUP BY
5️⃣ HAVING
6️⃣ SELECT (rAnk ALSO COMES HERE ONLYY)
7️⃣ DISTINCT
8️⃣ ORDER BY
9️⃣ LIMIT
SPARK:
#### What is the difference between partitioning and bucketing in big data systems, and when would you prefer one over the other?
Answer: 
      When to Use Bucketing
      Use when:
            High cardinality column
            Frequent joins on that column
            Want to optimize shuffle during joins
            Example:
             Joining two large tables on user_id.
            If both tables are bucketed on user_id with same number of buckets: Suffling will be less 
      Partitioning:
            Column has low cardinality
            Frequently used in filters
            Used in WHERE clause
            Partition pruning
            Reduces data scan
            Good for time-based data
### Difference between the repartition and coalasce
            Repartition: will do the full shuffle 
                        Can increase and decrease the shuffles 
                        used when all the data of same order needs to be in the same partition
                        equal distribution happens here ..
            Coalasce:
                        cheaper operation than the repartition 
                        can only decrease the no of partiotions 
                        used when we need to store the output to minimal no of partitions 
                        will also cause uneven distribution
                        

### Difference between the cache and the persist

            Cache: 
                  cached dfs are stored at the memory level only 
            Persit : can be done in multiple ways 
                     Allows choosing the storage level 
                     (MEMORY_ONLY,MEMORY_AND_DISK,DISK_ONLY,MEMORY_ONLY_SER)


### How do you handle the lately coming the data.

            df.withWatermark("event_time", "5 minutes") \
                  .groupBy(window("event_time", "10 minutes")) \
                    .count()

#### You need to join two tables with a big size difference: one is 2 TB, the other is 200 MB. How would you optimize this join?
Answe: 
            1. Brodcast Join
            2. if No 
            large_df = spark.read.parquet("/data/large_2tb_table")
            small_df = spark.read.parquet("/data/small_200mb_table")
            
            num_partitions = 2000
            
            large_df = large_df.repartition(num_partitions, col("join_key"))
            small_df = small_df.repartition(num_partitions, col("join_key"))
            
            joined_df = large_df.join(
                small_df,
                on="join_key",
                how="inner"
            )

### How would you detect and handle duplicate records while ingesting streaming data using PySpark or Apache Kafka?
dedup_df = parsed_df \
    .withWatermark("event_time", "10 minutes") \
    .dropDuplicates(["event_id"])




DATABRICICKS:
### Difference between the Delta Table and Delta Lake :
Answer:
      A table stored in Delta format using Delta Lake.
      Delta Lake is a storage framework built on top of data lakes (like S3, ADLS, HDFS).
      ACID transactions,Schema enforcement,Schema evolution,Time travel,Upserts (MERGE),Data versioning
## Difference between the data lake and the data warehouse 


| Feature           | Data Lake                       | Data Warehouse           |
| ----------------- | ------------------------------- | ------------------------ |
| Data Type         | Raw (any format)                | Structured               |
| Schema            | Schema-on-Read                  | Schema-on-Write          |
| Purpose           | Storage + Processing            | Analytics + Reporting    |
| Users             | Data Engineers, Data Scientists | Analysts, Business Users |
| Cost              | Low                             | Higher                   |
| Query Performance | Slower (needs processing)       | Faster for analytics     |
| Example           | Raw logs, JSON, IoT data        | Sales summary table      |



#### what is data lake :?
A Data Lake is a centralized storage system that stores raw data in its original format — structured, semi-structured, or unstructured.





### 
Difference between Auto Loader and Event Hubs — in what scenarios would you choose one over the other?
Answer:
      Event Hubs is a like Kafka 
      | Feature   | Auto Loader                        | Event Hubs                |
      | --------- | ---------------------------------- | ------------------------- |
      | Type      | File ingestion mechanism           | Streaming event broker    |
      | Platform  | Databricks                         | Azure                     |
      | Data Type | Files (JSON, CSV, Parquet, etc.)   | Real-time events/messages |
      | Nature    | Micro-batch streaming from storage | True event streaming      |
      | Use Case  | Landing zone ingestion             | Real-time event pipelines |
      
    Autoloader:
      1.Basically Autoloader is built on top of SPARK API .
      2. Optimized File Reading
      3. It is the most effiicient way of ingesting data into the Databricks from any cloud like ADLS , S3 ,Google storage and all  (because it is designed in such a way that with minimal calls Data will get stored)
      4. if any new file landed in the cloud storage it directly triggers
      df = (
              spark.readStream
                  .format("cloudFiles")
                  .option("cloudFiles.format", "csv")
                  .option("cloudFiles.inferColumnTypes", "true")
                  .option("cloudFiles.schemaLocation", schema_path)
                  .load(input_path)
          )

      5. More Suitable in Batch wise Processing
                                              
                                              
    Coming to the Event Hubs :
    1. if we get continous data and our data is Real Streaming we use this 
####
You receive files daily in ADLS. How would you automatically detect file arrival and identify corrupt files without stopping ingestion?

Anwer:

        df = (
        spark.readStream
            .format("cloudFiles")
            .option("cloudFiles.format", "json")
            .option("cloudFiles.schemaLocation", "dbfs:/schemas/")
            .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
            .option("badRecordsPath", "dbfs:/bad_records/")
            .load("abfss://raw@storageaccount.dfs.core.windows.net/data/")
    )
     I use Autooader for this when their is any schema evolution(mismatch) happens
     badrecords are stored in the badRecordsPath and it will be used for further inspection .

     and while storing we can use mergeschema =True in the Bronze layer 
     


1. How do you handle schema evolution in a data lake environment?

            ** spark.read.option("mergeSchema", "true").parquet("path")
            
DATA MODELING :
* Surrogate KEY ::
                  A Surrogate Key is an artificial (system-generated) unique ID given to each row in a dimension table.
                  It has no business meaning.

                  
                  | surrogate_key | customer_id | city      | start_date | end_date   | is_current |
                  | ------------- | ----------- | --------- | ---------- | ---------- | ---------- |
                  | 101           | C1          | Bangalore | 2024-01-01 | 2025-01-01 | N          |
                  | 205           | C1          | Hyderabad | 2025-01-01 | null       | Y          |
                  

 
 #### How would you design a slowly changing dimension (SCD) pipeline that supports Type 1 and Type 2 updates at high scale?
Answer:
Basicsally SCD are Slowly changing Dimension Tables  will store the Metainfo(info about the data ).
Based on the sitaution we chood the SCD 1 or Scd2 
if we need all the past changes in the Dimension Tables we use SCD 2 
where SCD2 will be having the 3 extra columns start_date,end_date,valid(Y/N)
SCD1 Directly replaces the previously existing record it wont store any history ....

For High Scale Data : 
                  1. I use Hash function for the SCD type 2 columns 
                  2. While Joining I only Join the Current_values Yes .
I can impleent SCD2 in the Spark for more clarity ...

### Implement SCD type 2 in the SPARK :
            from pyspark.sql import SparkSession
            from pyspark.sql.functions import (col, lit, current_date, expr)

           
            
            # 1. Spark Session
             # -------------------------------------------------------
            spark = SparkSession.builder.appName("SCD_Type_2_Customer").getOrCreate()
            
            # 2. Read Source (daily incoming data)
            # -------------------------------------------------------
            src_df = spark.read.parquet("/source/customer") 
            # Expected columns:
            # customer_id, name, city, phone
            # 3. Read Target Dimension Table
            tgt_df = spark.read.parquet("/dim/customer")
            # Expected columns:
            # customer_id, name, city, phone, start_date, end_date, is_current
            # 4. Filter current records from target
            # -------------------------------------------------------
            tgt_current_df = tgt_df.filter(col("is_current") == "Y")
            # -------------------------------------------------------
            
            # 5. Join source with current target
            # -------------------------------------------------------
            
            join_df = src_df.alias("src").join(tgt_current_df.alias("tgt"), on="customer_id", how="left")
            
            # ------------------------------------------------------
            # 6. Change detection logic
            # -------------------------------------------------------
            change_condition = ((col("src.name") != col("tgt.name")) |(col("src.city") != col("tgt.city")) |(col("src.phone") != col("tgt.phone")))
            
            # -------------------------------------------------------
            
            # 7. Expire old records (changed records)
            
            # -------------------------------------------------------
            
            expired_df = join_df.filter(change_condition & col("tgt.customer_id").isNotNull()).select( col("tgt.customer_id"),col("tgt.name"),col("tgt.city"),col("tgt.phone"),col("tgt.start_date")
).withColumn("end_date", current_date() - expr("INTERVAL 1 DAY")).withColumn("is_current", lit("N"))
            # 8. New records (new customers + changed customers)
            new_df = join_df.filter(change_condition | col("tgt.customer_id").isNull()).select(col("src.customer_id"),col("src.name"),col("src.city"),col("src.phone")).withColumn("start_date", current_date() ).withColumn("end_date", lit("9999-12-31")).withColumn("is_current", lit("Y"))
            # 9. Unchanged records
            
            # -------------------------------------------------------
             unchanged_df = tgt_current_df.join(
            
                expired_df.select("customer_id"),
            
                on="customer_id",
            
                how="left_anti"
            
            )
            final_dim_df = unchanged_df.unionByName(expired_df).unionByName(new_df)
 

Scenario Based Questions:

###  Explain how you would build a near real-time ingestion system for millions of events per minute. Which components would you choose and why?
Answer :
            Producers → Kafka → Spark Structured Streaming → Delta Lake → BI / ML / APIs

            1.Fault Tolerance is also acheived by Spark Structured Streaming .
            2.If Spark goes it restarts from the last commited offset  that is maintained through the checkpointing the spark 
            3.even kafka also maintains the consumer group offset but spark wont rely on top of it itself maintains through checkpointing 
##### You have a pipeline that runs every hour, but the downstream table shows duplicate records. Walk through your debugging approach 
step by step.

Answer:
            1. Checkpoint folder deleted
            2. Consumer group changed
            3. startingOffsets = earliest
            4. producer is sending the same records or what if yes I addd watermarking 
            5. creating the primary key in the delta table 
            6.Is offset committed only after successful write
            First, I would identify the type of duplicates and check whether they originate from source or processing.
            If it’s streaming, I would verify checkpointing and offset handling. 
            If it’s batch, I would check whether incremental logic is missing.
            I would then validate whether writes are append-only instead of merge-based. 
            Finally, I would ensure idempotency using event_id-based deduplication and Delta MERGE to prevent future duplicates.

### How would you design a data model for storing large-scale logs that need fast write speed and moderately fast reads?

Since we need the high write and moderate read we can use writing into parquet or DELTA lake unlike the any Database like
(CASSANDRA,mongoDB since here both write and read faster ) and not any Datawarehouse(not suitable for faster writes)
so we can use the HDFS or Delta Lake and write the data in the append 
and while storing the data we can make it partitions wrt yr/mt/dt so that read is also good performance assuming it is time related data 

 ####Describe how you handle schema evolution in a production pipeline. What happens if a producer adds a new column unexpectedly?
 Anwer:
            In  production, I separate schema flexibility and schema enforcement using a Medallion architecture.
            In the Bronze layer, I allow schema evolution using mergeSchema or Auto Loader’s addNewColumns mode so ingestion never fails. 
            In the Silver layer, I enforce a strict schema and validate data types.
            If a new column is added, I log and review it before propagating downstream. 
            If a datatype changes unexpectedly, I treat it as a schema violation and either quarantine those records or alert upstream producers. This ensures robustness and prevents silent data corruption.
            
            
### how do you handloe the schema evolution in the Avro,parquet and the json

      In Avro, schema evolution is managed through writer and reader schemas, usually with a Schema Registry, ensuring backward compatibility.
      In Parquet, schema is stored in file metadata, and new columns can be added, with schema merging enabled during reads.
      JSON is schema-less,so we enforce schema explicitly during ingestion to avoid inconsistencies.
      In production, we carefully manage type changes and use tools like Delta Lake or Iceberg for safer evolution.
      
      | Feature            | Avro       | Parquet   | JSON              |
      | ------------------ | ---------- | --------- | ----------------- |
      | Schema Stored      | Yes        | Yes       | No                |
      | Schema Enforcement | Strong     | Moderate  | None              |
      | Add Column         | Safe       | Safe      | Naturally handled |
      | Remove Column      | Safe       | Safe      | Naturally handled |
      | Type Change        | Restricted | Risky     | Runtime issues    |
      | Best Use           | Streaming  | Analytics | Raw ingestion     |

      JSON: spark.read.schema(predefined_schema).json("path")
      Parquet: spark.read.option("mergeSchema", "true").parquet("path")
      AVRO : Schema is stored with the data (or in Schema Registry)
            writer schema , reader schema stored seperately ..
      

Uses writer schema and reader schema


** check the schema registory part..



### how do you handle the schema evolution inthe Data warehouses like snowflake and redshioft

            * will be using the alter commands in case of the to add the new columns
            * for renaming column or changing datatype I will create the new column and backfill the data ..]
            * mostly I will try to drop the table .
            if it is mandatory I will do ->Create new column with correct type 
                                          Migrate data gradually

### How do you makesure that ur datawarehouse is not having the Duplicats 

            1. ✅ Primary Key Constraints
            2. If Duplicates exists.->use merge statements instead of direct insert statements.
            3.use the merge statements instead of directly inserting the records .

                  MERGE INTO sales t
                  USING staging_sales s
                  ON t.order_id = s.order_id
                  
                  WHEN MATCHED THEN
                      UPDATE SET
                          t.amount = s.amount,
                          t.updated_at = s.updated_at
                  
                  WHEN NOT MATCHED THEN
                      INSERT (order_id, amount, updated_at)
                      VALUES (s.order_id, s.amount, s.updated_at);

## what is the role of t he of dataengineers.in OLAP to OLTP 
            OLTP Database → ETL/ELT → Data Lake/Warehouse (OLAP)

            Data Engineers:
            Extract data from OLTP systems
            Transform and clean data
            Load into OLAP systems
            Optimize for analytics












